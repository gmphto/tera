{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The core permissions the main window needs, plus the one dialog permission the folder picker uses. No message, save or confirm dialog is reachable.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
