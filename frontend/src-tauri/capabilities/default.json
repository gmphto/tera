{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The core permissions the main window needs. No plugin permission is granted, because no plugin is installed.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
